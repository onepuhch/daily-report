# Step 2: Validate Output

## 목표
추출된 데이터의 결측치와 이상치를 검증한다.

## 작업
- `#N/A`, 빈 값, 계산 오류를 탐지한다.
- 전일대비와 전년말대비가 표시 단위에 맞는지 확인한다.
- 실패 시 어떤 시트/셀에서 문제가 났는지 로그를 남긴다.

## AC
- 추출 성공/실패를 사람이 이해할 수 있는 로그로 확인할 수 있다.
- 문제 데이터가 있어도 전체 파이프라인이 조용히 틀린 결과를 내지 않는다.


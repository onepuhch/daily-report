# Step 1: Extract Report Values

## 목표
갱신된 `MARKET DAILY.xlsm`에서 리포트에 필요한 값을 추출한다.

## 작업
- `camera` 시트의 리포트용 표 범위를 우선 추출한다.
- 필요 시 원천 시트의 데이터도 함께 추출한다.
- 날짜, 지표명, 값, 전일대비, 전년말대비를 표준 JSON/CSV로 저장한다.

## AC
- 하루치 리포트 숫자를 파일로 추출할 수 있다.
- 각 값의 원천 시트와 셀 주소를 추적할 수 있다.


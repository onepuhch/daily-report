# Step 2: Finalize Data Model

## 목표
Supabase PostgreSQL + pgvector에 저장할 테이블 구조를 확정한다.

## 작업
- 숫자 데이터 테이블을 확정한다.
- 코멘트/참고자료/임베딩 테이블을 확정한다.
- 관리자 화면에서 수정할 필드를 확정한다.

## AC
- `docs/DATA_MODEL.md`가 실제 구현 가능한 스키마 수준으로 구체화되어 있다.
- SQL 검색과 Vector 검색의 역할이 분리되어 있다.


# Step 1: Map Excel Metrics

## 목표
`MARKET DAILY.xlsm`에서 리포트에 필요한 지표, 시트, 셀 주소를 매핑한다.

## 작업
- `camera` 시트를 우선 분석한다.
- 원천 시트의 인포맥스 함수와 표시명을 정리한다.
- 추출 대상 지표의 카테고리, 표시명, 단위, 전일대비 계산 방식을 정의한다.

## AC
- `config/metrics.yml`에 핵심 지표 목록이 정리되어 있다.
- 각 지표의 원천 시트와 셀 또는 범위가 정의되어 있다.

